#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require "mind_breaker"

app = MindBreaker::App.new(STDIN, STDOUT, "SlidingPuzzle")
app.start
app.greet
app.create_agent

puts app.agent.goal.inspect
puts app.agent.inspect
# Handle the args here
# ARGV.each do |arg|
#   puts arg
# end